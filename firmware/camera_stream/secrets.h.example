// Copy this file to `secrets.h` and fill in your WiFi credentials.
// IMPORTANT: Do not commit `secrets.h` (it's ignored via .gitignore).

#pragma once

// Home WiFi (priority 1 - will try this first)
#define HOME_WIFI_SSID "YOUR_HOME_WIFI_SSID_HERE"
#define HOME_WIFI_PASSWORD "YOUR_HOME_WIFI_PASSWORD_HERE"

// iPhone hotspot fallback (priority 2 - used if home WiFi unavailable)
#define HOTSPOT_SSID "YOUR_IPHONE_HOTSPOT_SSID_HERE"
#define HOTSPOT_PASSWORD "YOUR_HOTSPOT_PASSWORD_HERE"
